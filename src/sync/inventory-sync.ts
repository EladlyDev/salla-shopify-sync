export async function syncShopifyInventoryToSalla(
    shopifyInventoryItemId: number,
    shopifyLocationId: number
): Promise<void> {
    console.log(`[INV-SYNC] TODO: sync inventory item ${shopifyInventoryItemId} at location ${shopifyLocationId}`);
}
